#!/usr/bin/env bash
# Doble clic en este archivo instala Cacho. (Es `instalar.sh`, pero macOS abre los
# archivos .command en la Terminal solo — sin tener que saber qué es una Terminal.)
cd "$(dirname "$0")"
bash instalar.sh
echo ""
echo "Ya podés cerrar esta ventana."
